<UserControl
    x:Class="SpatialLabsOptimizer.Views.ViewingDistanceCoachView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Spacing="12" MaxWidth="480">
        <TextBlock Text="Viewing distance coach" FontSize="20" FontWeight="SemiBold" />
        <TextBlock x:Name="TipBlock" TextWrapping="Wrap" Opacity="0.85" />
        <Slider x:Name="DistanceSlider" Header="Your distance (cm)" Minimum="40" Maximum="120" Value="65" StepFrequency="1" ValueChanged="DistanceSlider_ValueChanged" />
        <TextBlock x:Name="FeedbackBlock" TextWrapping="Wrap" FontWeight="SemiBold" />
        <TextBlock x:Name="RangeBlock" Opacity="0.7" />
    </StackPanel>
</UserControl>
