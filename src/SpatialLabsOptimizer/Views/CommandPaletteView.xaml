<Page
    x:Class="SpatialLabsOptimizer.Views.CommandPaletteView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:pcvr="using:SpatialLabsOptimizer.Infrastructure.Pcvr">
    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Quick Actions" FontSize="28" FontWeight="SemiBold" />
        <TextBlock Grid.Row="1" Text="Search shortcuts for library refresh, logs, safe launch, diagnostics, and more. Press Ctrl+K anywhere in the app." TextWrapping="Wrap" Opacity="0.8" />
        <TextBox x:Name="SearchBox"
                 Grid.Row="2"
                 AutomationProperties.AutomationId="CommandPalette_Search"
                 PlaceholderText="Search quick actions…"
                 TextChanged="SearchBox_TextChanged" />
        <ListView x:Name="ResultsList" Grid.Row="3" SelectionMode="Single" IsItemClickEnabled="True" ItemClick="ResultsList_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="pcvr:CommandPaletteEntry">
                    <StackPanel Spacing="2" Padding="4">
                        <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold" />
                        <TextBlock Text="{x:Bind Description}" Opacity="0.75" TextWrapping="Wrap" FontSize="12" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
