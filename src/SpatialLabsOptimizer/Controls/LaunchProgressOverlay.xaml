<UserControl
    x:Class="SpatialLabsOptimizer.Controls.LaunchProgressOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Visibility="Collapsed">
    <Grid Background="#CC000000">
        <Border
            Background="{StaticResource Brush.Surface}"
            CornerRadius="12"
            Padding="24"
            Width="480"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <StackPanel Spacing="16">
                <TextBlock Text="Launching in 3D" FontSize="20" FontWeight="SemiBold" />
                <TextBlock x:Name="GameTitleBlock" FontSize="16" Opacity="0.9" />
                <ProgressBar x:Name="LaunchProgressBar" IsIndeterminate="True" />
                <TextBlock x:Name="StepBlock" TextWrapping="Wrap" />
                <ItemsRepeater x:Name="StepsRepeater">
                    <ItemsRepeater.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Opacity="0.8" />
                        </DataTemplate>
                    </ItemsRepeater.ItemTemplate>
                </ItemsRepeater>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
